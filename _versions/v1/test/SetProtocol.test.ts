import * as chai from "chai";

const { expect } = chai;

// SetProtocol.test.ts
describe(`SetProtocol`, () => {
  // TODO: Add more tests
  it(`should run test suite`, () => {
    expect(1).to.equal(1);
  });
});

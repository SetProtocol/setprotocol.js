import {
  addressSchema,
  bytes32Schema,
  bytesSchema,
  numberSchema,
  wholeNumberSchema,
} from "./basic_type_schemas";

export const Schemas = {
  addressSchema,
  numberSchema,
  bytes32Schema,
  bytesSchema,
  wholeNumberSchema,
};

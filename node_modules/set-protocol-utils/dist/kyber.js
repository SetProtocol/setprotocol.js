/*
  Copyright 2018 Set Labs Inc.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
*/
'use strict';
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var _ = __importStar(require("lodash"));
var ethUtil = __importStar(require("ethereumjs-util"));
var constants_1 = require("./constants");
var encoding_1 = require("./encoding");
var orders_1 = require("./orders");
/* ============ Kyber Trades Functions ============ */
/**
 * Takes Kyber trades and generates a buffer representing all trades
 *
 * @param  trades              Array of KyberTrade interface
 * @return                     Entire kyber trades data as a buffer
 */
function generateKyberTradesBuffer(trades) {
    var kyberTradesAsBuffers = [];
    // Turn all Kyber trades to buffer
    _.map(trades, function (trade) {
        kyberTradesAsBuffers.push(kyberTradeToBuffer(trade));
    });
    var kyberTradesBuffer = Buffer.concat(kyberTradesAsBuffers);
    // Generate header for Kyber trades
    var kyberTradesHeader = Buffer.concat(orders_1.generateExchangeOrderHeader(constants_1.constants.EXCHANGES.KYBER, trades.length, kyberTradesBuffer.length));
    return Buffer.concat([kyberTradesHeader, kyberTradesBuffer]);
}
exports.generateKyberTradesBuffer = generateKyberTradesBuffer;
/**
 * Takes a Kyber trade object and turns it in hex form. Used to test KyberNetworkWrapper where
 * exchange header is not necessary
 *
 * @param  trade   Object conforming to Kyber trade
 * @return         Kyber trade as a hex string
 */
function kyberTradesToBytes(trade) {
    return ethUtil.bufferToHex(kyberTradeToBuffer(trade));
}
exports.kyberTradesToBytes = kyberTradesToBytes;
/**
 * Takes a Kyber trade object and turns it into a buffer
 *
 * @param  trade   Object conforming to Kyber trade
 * @return         Kyber trade as a buffer
 */
function kyberTradeToBuffer(trade) {
    var kyberSwapBuffer = [];
    kyberSwapBuffer.push(encoding_1.paddedBufferForPrimitive(trade.destinationToken));
    kyberSwapBuffer.push(encoding_1.paddedBufferForPrimitive(trade.sourceToken));
    kyberSwapBuffer.push(encoding_1.paddedBufferForBigNumber(trade.sourceTokenQuantity));
    kyberSwapBuffer.push(encoding_1.paddedBufferForBigNumber(trade.minimumConversionRate));
    kyberSwapBuffer.push(encoding_1.paddedBufferForBigNumber(trade.maxDestinationQuantity));
    return Buffer.concat(kyberSwapBuffer);
}
//# sourceMappingURL=kyber.js.map
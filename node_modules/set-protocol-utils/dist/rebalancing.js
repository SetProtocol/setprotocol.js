/*
  Copyright 2018 Set Labs Inc.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
*/
'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var encoding_1 = require("./encoding");
/**
 * Function for clarifying the additional call data parameters that need to be sent to Core when creating
 * a new rebalancing set token
 *
 * @param  managerAddress      Address of the manager to manage the rebalancing
 * @param  proposalPeriod      Time the participants of the Set can withdraw from a rebalance
 *                               once a new Set has been proposed
 * @param  rebalanceInterval   Time between when the manager can initiate another rebalance
 * @return                     String representing call data to send to Core contracts
 */
function generateRebalancingSetTokenCallData(managerAddress, proposalPeriod, rebalanceInterval) {
    return encoding_1.bufferArrayToHex([
        encoding_1.paddedBufferForPrimitive(managerAddress),
        encoding_1.paddedBufferForBigNumber(proposalPeriod),
        encoding_1.paddedBufferForBigNumber(rebalanceInterval),
    ]);
}
exports.generateRebalancingSetTokenCallData = generateRebalancingSetTokenCallData;
/**
 * Function for clarifying the additional call data parameters that need to be sent to Core when creating
 * a new rebalancing set token
 *
 * @param  managerAddress           Address of the manager to manage the rebalancing
 * @param  liquidatorAddress        Address of liquidator that handles rebalancing
 * @param  recipientAddress         Address of recipient of fees
 * @param  rebalanceInterval        Time between when the manager can initiate another rebalance
 * @param  lastRebalanceTimestamp   Customized time in seconds of the last rebalance
 * @param  entryFee                 Mint fee in scaled value (10e18 value)
 * @param  rebalanceFee             Rebalance fee in scaled value
 * @return                          String representing call data to send to Core contracts
 */
function generateRebalancingSetTokenV2CallData(managerAddress, liquidatorAddress, feeRecipient, rebalanceFeeCalculator, rebalanceInterval, failRebalancePeriod, lastRebalanceTimestamp, entryFee, rebalanceFeeCalculatorCalldata) {
    return encoding_1.bufferArrayToHex([
        encoding_1.paddedBufferForPrimitive(managerAddress),
        encoding_1.paddedBufferForPrimitive(liquidatorAddress),
        encoding_1.paddedBufferForPrimitive(feeRecipient),
        encoding_1.paddedBufferForPrimitive(rebalanceFeeCalculator),
        encoding_1.paddedBufferForBigNumber(rebalanceInterval),
        encoding_1.paddedBufferForBigNumber(failRebalancePeriod),
        encoding_1.paddedBufferForBigNumber(lastRebalanceTimestamp),
        encoding_1.paddedBufferForBigNumber(entryFee),
        rebalanceFeeCalculatorCalldata,
    ]);
}
exports.generateRebalancingSetTokenV2CallData = generateRebalancingSetTokenV2CallData;
/**
 * Function for clarifying the additional call data parameters that need to be sent to Core when creating
 * a new rebalancing set token v3
 *
 * @param  managerAddress           Address of the manager to manage the rebalancing
 * @param  liquidatorAddress        Address of liquidator that handles rebalancing
 * @param  recipientAddress         Address of recipient of fees
 * @param  rebalanceInterval        Time between when the manager can initiate another rebalance
 * @param  lastRebalanceTimestamp   Customized time in seconds of the last rebalance
 * @param  entryFee                 Mint fee in scaled value (10e18 value)
 * @param  feeCalculatorCalldata    Fee calculator in array of buffers
 * @return                          String representing call data to send to Core contracts
 */
function generateRebalancingSetTokenV3CallData(managerAddress, liquidatorAddress, feeRecipient, rebalanceFeeCalculator, rebalanceInterval, failRebalancePeriod, lastRebalanceTimestamp, entryFee, feeCalculatorCalldata) {
    return encoding_1.bufferArrayToHex([
        encoding_1.paddedBufferForPrimitive(managerAddress),
        encoding_1.paddedBufferForPrimitive(liquidatorAddress),
        encoding_1.paddedBufferForPrimitive(feeRecipient),
        encoding_1.paddedBufferForPrimitive(rebalanceFeeCalculator),
        encoding_1.paddedBufferForBigNumber(rebalanceInterval),
        encoding_1.paddedBufferForBigNumber(failRebalancePeriod),
        encoding_1.paddedBufferForBigNumber(lastRebalanceTimestamp),
        encoding_1.paddedBufferForBigNumber(entryFee),
    ].concat(feeCalculatorCalldata));
}
exports.generateRebalancingSetTokenV3CallData = generateRebalancingSetTokenV3CallData;
/**
 * Function for generating Buffer required for RebalancingSetTokenV2 creation. The main input
 * is the rebalance fee.
 *
 * @param  rebalanceFee             Rebalance fee in scaled value
 * @return                          String representing fixed fee calc data
 */
function generateFixedFeeCalculatorCalldata(rebalanceFee) {
    return encoding_1.paddedBufferForBigNumber(rebalanceFee);
}
exports.generateFixedFeeCalculatorCalldata = generateFixedFeeCalculatorCalldata;
/**
 * Function for generating hex string that can be parsed by performance fee calculator to set fee params.
 *
 * @param  profitPeriod                   Time allowed between profitFee collection
 * @param  highWatermarkResetPeriod       Time required to pass from last profitFee accrual before highWatermark is
 *                                        reset
 * @param  profitFeePercentage            Profit fee percentage
 * @param  streamingFeePercentage         Streaming fee percentage
 * @return                                String representing performance fee calc data
 */
function generatePerformanceFeeCallData(profitPeriod, highWatermarkResetPeriod, profitFeePercentage, streamingFeePercentage) {
    return encoding_1.bufferArrayToHex([
        encoding_1.paddedBufferForBigNumber(profitPeriod),
        encoding_1.paddedBufferForBigNumber(highWatermarkResetPeriod),
        encoding_1.paddedBufferForBigNumber(profitFeePercentage),
        encoding_1.paddedBufferForBigNumber(streamingFeePercentage),
    ]);
}
exports.generatePerformanceFeeCallData = generatePerformanceFeeCallData;
/**
 * Function for generating performance fee calculator buffer array that can be appended to other call data to form
 * inputs for RebalancingSetTokenV3.
 *
 * @param  profitPeriod                   Time allowed between profitFee collection
 * @param  highWatermarkResetPeriod       Time required to pass from last profitFee accrual before highWatermark is
 *                                        reset
 * @param  profitFeePercentage            Profit fee percentage
 * @param  streamingFeePercentage         Streaming fee percentage
 * @return                                Array of performance fee calc buffers that can be appended to other call data
 */
function generatePerformanceFeeCallDataBuffer(profitPeriod, highWatermarkResetPeriod, profitFeePercentage, streamingFeePercentage) {
    return [
        encoding_1.paddedBufferForBigNumber(profitPeriod),
        encoding_1.paddedBufferForBigNumber(highWatermarkResetPeriod),
        encoding_1.paddedBufferForBigNumber(profitFeePercentage),
        encoding_1.paddedBufferForBigNumber(streamingFeePercentage),
    ];
}
exports.generatePerformanceFeeCallDataBuffer = generatePerformanceFeeCallDataBuffer;
/**
 * Function for generating hex string that can be parsed by performance fee calculator to adjust fee params.
 *
 * @param  feeType                  Enumeration of fee being changed
 * @param  newFeePercentage         New fee percentage
 * @return                          String representing adjust fee data
 */
function generateAdjustFeeCallData(feeType, newFeePercentage) {
    return encoding_1.bufferArrayToHex([
        encoding_1.paddedBufferForBigNumber(feeType),
        encoding_1.paddedBufferForBigNumber(newFeePercentage),
    ]);
}
exports.generateAdjustFeeCallData = generateAdjustFeeCallData;
//# sourceMappingURL=rebalancing.js.map
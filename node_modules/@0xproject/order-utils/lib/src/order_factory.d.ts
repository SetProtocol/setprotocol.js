import { Order, SignedOrder } from '@0xproject/types';
import { BigNumber } from '@0xproject/utils';
import { Provider } from 'ethereum-types';
import { CreateOrderOpts } from './types';
export declare const orderFactory: {
    createOrderFromPartial(partialOrder: Partial<Order>): Order;
    createSignedOrderFromPartial(partialSignedOrder: Partial<SignedOrder>): SignedOrder;
    createOrder(makerAddress: string, makerAssetAmount: BigNumber, makerAssetData: string, takerAssetAmount: BigNumber, takerAssetData: string, exchangeAddress: string, createOrderOpts?: CreateOrderOpts): Order;
    createSignedOrderAsync(provider: Provider, makerAddress: string, makerAssetAmount: BigNumber, makerAssetData: string, takerAssetAmount: BigNumber, takerAssetData: string, exchangeAddress: string, createOrderOpts?: CreateOrderOpts | undefined): Promise<SignedOrder>;
};
//# sourceMappingURL=order_factory.d.ts.map
import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { LinkEx } from "./LinkEx";
export declare class LinkExFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(): Promise<LinkEx>;
    getDeployTransaction(): UnsignedTransaction;
    attach(address: string): LinkEx;
    connect(signer: Signer): LinkExFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): LinkEx;
}

import { BigNumber } from 'bignumber.js';
import { Address } from 'set-protocol-utils';
import { RebalancingSetEthBidderContract, RebalancingSetCTokenBidderContract } from '../contracts';
export declare class RebalancingSetBidderHelper {
    private _contractOwnerAddress;
    constructor(contractOwnerAddress: Address);
    deployRebalancingSetEthBidderAsync(rebalanceAuctionModuleAddress: Address, transferProxyAddress: Address, wethAddress: Address, from?: Address): Promise<RebalancingSetEthBidderContract>;
    deployRebalancingSetCTokenBidderAsync(rebalanceAuctionModuleAddress: Address, transferProxyAddress: Address, cTokenAddressesArray: Address[], underlyingAddressesArray: Address[], dataDescription: string, from?: Address): Promise<RebalancingSetCTokenBidderContract>;
    replaceFlowsWithCTokenUnderlyingAsync(expectedTokenFlows: any, combinedTokenArray: Address[], cTokenAddressesArray: Address[], underlyingAddressesArray: Address[], cTokenExchangeRateArray: BigNumber[]): any;
    constructObjectFromArray(array1: any[], array2: any[]): any;
}

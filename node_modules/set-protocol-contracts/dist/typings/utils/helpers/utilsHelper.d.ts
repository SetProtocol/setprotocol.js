import { Address } from 'set-protocol-utils';
import { AddressToAddressWhiteListContract } from '../contracts';
export declare class UtilsHelper {
    private _tokenOwnerAddress;
    constructor(tokenOwnerAddress: Address);
    deployAddressToAddressWhiteListAsync(initialKeyTypeAddresses?: Address[], initialValueTypeAddresses?: Address[], from?: Address): Promise<AddressToAddressWhiteListContract>;
}

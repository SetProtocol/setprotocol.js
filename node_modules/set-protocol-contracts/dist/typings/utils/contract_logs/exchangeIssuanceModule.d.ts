import { Address, Log } from 'set-protocol-utils';
import { BigNumber } from 'bignumber.js';
export declare function LogExchangeIssue(setAddress: Address, callerAddress: Address, quantity: BigNumber, sendTokens: Address[], sendTokenAmounts: BigNumber[], contractAddress: Address): Log[];
export declare function LogExchangeRedeem(setAddress: Address, callerAddress: Address, quantity: BigNumber, receiveTokens: Address[], receiveTokenAmounts: BigNumber[], contractAddress: Address): Log[];

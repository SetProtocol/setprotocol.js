import { BigNumber } from '.';
export declare const calculatePartialAmount: (quantity: BigNumber, numerator: BigNumber, denominator: BigNumber) => BigNumber;
export declare const calculatePercentDifference: (newNumber: BigNumber, originalNumber: BigNumber) => BigNumber;
